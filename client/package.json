{
  "name": "@memstate/mcp",
  "version": "0.5.1",
  "description": "memstate — MCP stdio front-end for a self-hosted Go daemon with versioned SQLite memory.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "memstate-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node dist/index.js --test"
  },
  "files": [
    "dist/",
    "src/",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "mcp",
    "memory",
    "agent-memory",
    "versioned-memory",
    "keypath",
    "self-hosted",
    "sqlite"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.0.0"
  }
}
